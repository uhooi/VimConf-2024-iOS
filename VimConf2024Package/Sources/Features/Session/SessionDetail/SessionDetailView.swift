import SwiftUI
import SessionData

// MARK: - Actions

enum SessionDetailViewAction {
}

enum SessionDetailViewAsyncAction {
}

// MARK: - View

struct SessionDetailView: View {
    let session: Session

    var body: some View {
        Text("TODO: ")
    }
}
